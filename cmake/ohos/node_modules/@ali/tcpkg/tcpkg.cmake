# MNN invokes export_headers() while configuring for OHOS. The function belongs
# to Alibaba's internal tcpkg publishing environment and is not part of the
# runtime build. Keep the standalone SDK build independent of that unpublished
# package by providing the packaging hook as an intentional no-op.
function(export_headers)
endfunction()
